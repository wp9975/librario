import 'package:flutter/material.dart';

class player extends StatelessWidget {
  const player({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Słuchanie'),
    );
  }
}
